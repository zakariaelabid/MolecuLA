# Data

`smiles_selfies_full.csv` contains the SELFIES/SMILES molecule table used by the study. The required columns are:

- `smiles`
- `selfies`

`selfies_tokenizer.json` stores the tokenizer used by the included non-autoregressive checkpoints.

Use Git LFS for the CSV when publishing this repository.
